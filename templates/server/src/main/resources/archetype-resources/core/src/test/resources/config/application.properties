# This is the spring boot configuration file for JUnit tests. It will only affect JUnits and is not included into the application.
spring.profiles.active=junit

# Database and JPA
spring.jpa.database=h2
spring.datasource.url=jdbc:h2:mem:app;
spring.datasource.password=
spring.datasource.username=sa
spring.jpa.hibernate.ddl-auto=none

# Configuration for Database Setup and Migrations
#if ($dbMigration == 'flyway')
spring.flyway.enabled=true
#if ($dbType == 'h2')
spring.flyway.locations=classpath:db/migration
#else
spring.flyway.locations=classpath:db/test
#end
#end
#if ($dbMigration == 'liquibase')
spring.liquibase.enabled=true
#if ($dbType == 'h2')
spring.liquibase.change-log=classpath:db/changelog
#else
spring.liquibase.change-log=classpath:db/test
#end
#end